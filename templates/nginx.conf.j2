server {
	listen 80;
        server_name {{ ansible_play_hosts.0 }};
        client_max_body_size {{ client_max_body_size }};

	location / {
		proxy_set_header   X-Forwarded-For $remote_addr;
		proxy_set_header   Host $http_host;
		proxy_pass         "http://127.0.0.1:{{ irida_port }}/";
	}
}
